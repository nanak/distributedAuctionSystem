package Server;

import java.net.ServerSocket;

/**
 * Manages a connection from a user
 * @author Tobi
 *
 */
public class ManageConnection {
 
	private User user;
	
	/**
	 * socket of the 
	 * @param socket
	 * @param user
	 */
	public  ManageConnection(ServerSocket socket, User user) {
	 
	}
	 
}
 
